/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.genjutsu.id.vn/',
  generateRobotsTxt: true, // Tự động tạo robots.txt
  // (Tùy chọn khác: exclude, sitemapSize, ...)
}
